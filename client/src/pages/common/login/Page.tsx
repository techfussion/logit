import Login from "@/components/layout//Login";

const LoginPage: React.FC = () => {
    return (
        <div className="bg-customGray3 w-screen h-screen flex items-center justify-center">
            <Login />
        </div>
    );
}

export default LoginPage;